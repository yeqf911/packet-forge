1. 界面所有文字都使用英文展示，不需要有中文
2. 在消息发送界面，可以选择不同的协议类型，协议要支持预设，选择协议后应用，应当出现协议中所有的字段供用户填写
3. 字段中可能会有变长数据，这时不需要指定字段长度（或者是字节数）
4. 回写的消息展示分为两部分，分为左右两部分，左边展示十六进制字节数据，右边展示实际的解码后的数据，例如字符串
5. 界面的区块分割线不要太明显，可做成与背景色相近的颜色，不要白色或者浅色								