import { Typography } from 'antd';

const { Title } = Typography;

export default function Protocols() {
  return (
    <div style={{ padding: '24px' }}>
      <Title level={2}>Protocol Configuration</Title>
      <p>Protocol configuration feature is under development...</p>
    </div>
  );
}
